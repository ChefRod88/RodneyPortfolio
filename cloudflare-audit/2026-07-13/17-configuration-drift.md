# Configuration Drift

* **Wrangler config vs. Deployed configuration**:
  * `wrangler.json` specifies `./wwwroot` as the asset directory with `not_found_handling` and `html_handling`.
  * Deployed configuration is **aligned** (success).