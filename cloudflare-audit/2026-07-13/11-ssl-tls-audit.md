# SSL/TLS Audit

* **SSL Mode**: Full (Strict) - active.
* **Always Use HTTPS**: Enabled (Verified).
* **TLS version**: Minimum TLS 1.2 enforced. TLS 1.3 is enabled.
* **HTTPS Redirect**: Works flawlessly from the public internet.